[package]
name = "dfprobe"
version = "0.1.0"
edition = "2021"
[dependencies]
datafusion = "=55.0.0"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
[profile.dev]
opt-level = 1

# Standalone: not a member of the burrmill workspace.
[workspace]
