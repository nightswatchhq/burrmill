
A4: plan shapes in a real workload
  files          126
  statements     65
  unparsed files 0
  DISTINCT SHAPES  32  (expression granularity: which aggregates, casts, arithmetic)
  PLAN FAMILIES    22  (what an operator actually has to be)
  admitted today  0/65  (0.0% coverage ratio, §4.6)
  top 10 shapes cover 35/65 (54%)
  top 5 families cover 31/65 (48%)

  FOLD SUB-PLANS  8/8 admitted  (100%)  <- what an owned
           operator can execute today. Statement-level coverage stays at 0/65
           because every one of these folds sits inside a CTE or a join.

  n-table signed folds: 8/65 statements, branch counts ["2x5", "4x2", "5x1"]
  Of those, 4 read ONE table - the ERC-20 `Transfer` shape, where a single row
           carries both a payer and a payee - and 4 read several, because for most events a
           credit and a debit are different events and therefore different tables. The first version of
           this said the one-table shape occurred ZERO times, which was wrong: the detector counted
           union arms without checking whether the tables differed.

  count  plan family
     11  group-by+agg
      7  scan-only
      5  set-op
      4  cte+set-op+join+distinct+group-by+agg
      4  join
      4  set-op+group-by+agg
      3  cte+set-op+join+window
      3  join+group-by+agg
      3  join+subquery+group-by+agg
      3  set-op+join+group-by+agg
      3  subquery+agg
      3  subquery+group-by+agg
      2  group-by+having+agg
      2  window
      1  cte+join+group-by+agg
      1  cte+set-op+join
      1  cte+set-op+join+group-by+agg
      1  cte+set-op+join+window+group-by+agg
      1  cte+set-op+subquery+group-by+agg
      1  cte+set-op+window+group-by+agg
      1  cte+subquery+window
      1  cte+window+group-by+agg

  count  shape
      5  cast
      4  set-op+cast
      4  set-op+join+sum+count+min/max+group-by+distinct+cte+cast
      4  set-op+sum+group-by+cast
      3  join+arith+cast
      3  join+subquery+count+min/max+group-by+arith+cast
      3  set-op+join+count+group-by+order-by
      3  set-op+join+window+cte+case
      3  subquery+count
      3  sum+count+group-by
      2  count+count-distinct+min/max+group-by
      2  count+min/max+group-by
      2  count+min/max+group-by+having+order-by
      2  join+sum+count+count-distinct+min/max+group-by
      2  scan-only
      2  subquery+count+group-by+arith
      2  sum+count+count-distinct+min/max+group-by
      2  window+cast
      1  join+min/max+group-by
      1  join+order-by+arith+cast
      1  join+sum+count-distinct+group-by+cte+arith+cast
      1  set-op
      1  set-op+join+cte+cast
      1  set-op+join+sum+group-by+order-by+cte+cast
      1  set-op+join+sum+group-by+window+cte+case+cast
      1  set-op+min/max+group-by+window+cte+arith+cast
      1  set-op+subquery+sum+count+count-distinct+min/max+group-by+order-by+limit+cte+cast
      1  subquery+count+count-distinct+group-by
      1  subquery+window+cte+cast
      1  sum+count+group-by+cast
      1  sum+count+min/max+group-by+cast
      1  sum+count+min/max+group-by+order-by+window+cte

  count  why the planner refuses
     36  the fold projects at least one group key and at least one SUM; found no 
     15  common table expressions are not in the admitted subset
      7  SUM must be applied to a bare column
      5  the outer query must be a SELECT
      2  the only admitted aggregate in this shape is SUM; got `min`
